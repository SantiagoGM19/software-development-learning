/**
 * 1. Multiples & Exclusions (for loop)
 * Write a function sumDivisible(limit) that uses a for loop to find the sum of all positive integers up to limit (inclusive) 
 * that are divisible by 3 or 5, but not divisible by both (exclude numbers divisible by 15).
 * Input: 16
 * Expected Output: 45 (3 + 5 + 6 + 9 + 10 + 12 = 45; 15 is excluded)
 */
function sumDivisible(limit) {
    // Write your code here
}

/**
 * 2. Digit Counter (while loop)
 * Write a function countDigits(num) that uses a while loop to determine how many digits a
 * non-negative integer contains by repeatedly stripping off the last digit with
 * Math.floor(num / 10) or modulo arithmetic. Do not convert the number to a string or array.
 * 
 * Input: 98452
 * Expected Output: 5
 * Edge case: 0 should return 1
 */
function countDigits(n) {
    // Write your code here
}

/**
 * 3. Integer Palindrome Checker (while loop)
 * Write a function isNumberPalindrome(n) that determines if an integer reads the same
 * backward as forward without converting it to a string or array. Use a while loop to extract
 * digits using % 10 and reconstruct the reversed number algebraically (reversed = reversed * 10 + digit), then compare. 
 * Negative numbers are not palindromes.
 * 
 * Input: 12321
 * Expected Output: true
 * 
 * Input: -121
 * Expected Output: false
 */
function isNumberPalindrome(n) {
    // Write your code here
}
